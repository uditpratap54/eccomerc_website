<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body>
    <%- include('../partials/navbar') %>
    <main class="container">
      <section class="card" style="max-width:460px; margin:24px auto;">
        <h2 style="margin-bottom:12px;">Login</h2>
        <% if (flash && flash.type==='error') { %>
          <div class="alert error"><%= flash.message %></div>
        <% } %>
        <form action="/login" method="post" class="form-grid">
          <label>Email</label>
          <input type="email" name="email" placeholder="you@example.com" required />

          <label>Password</label>
          <input type="password" name="password" placeholder="••••••••" required />

          <button type="submit" class="btn" style="margin-top:12px;">Login</button>
        </form>
        <p class="muted" style="margin-top:8px;">New here? <a href="/register">Create an account</a></p>
      </section>
    </main>
    <%- include('../partials/footer') %>
  </body>
</html>